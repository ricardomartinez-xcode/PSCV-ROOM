import assert from"node:assert/strict";import{readFileSync}from"node:fs";import test from"node:test";const s=readFileSync(new URL("../components/app-shell-v5.tsx",import.meta.url),"utf8"),c=readFileSync(new URL("../app/events-flow.css",import.meta.url),"utf8");test("multi-day events color every day",()=>{assert.match(s,/function eventIncludesDate/);assert.match(s,/date >= start && date <= end/);assert.match(s,/calendarEventItem/);assert.match(c,/content:"Evento"/)});
