import { ACTIVITY_REMINDER_KINDS, syncEventReminders } from "@/lib/server/event-reminders";
type Row={id:string;title:string;due_date:string;due_time:string;task_type_id:string|null;item_kind:"task"|"event"|null;starts_at:string|null;ends_at:string|null};
export async function ensureAutomaticReminders(env:CloudflareEnv){const start=new Date().toISOString().slice(0,10);const end=new Date(Date.now()+120*86400000).toISOString().slice(0,10);const rows=await env.DB.prepare(`SELECT id,title,due_date,due_time,task_type_id,item_kind,starts_at,ends_at FROM tasks WHERE archived_at IS NULL AND status NOT IN ('Entregado','Cancelado') AND due_date BETWEEN ? AND ? ORDER BY due_date LIMIT 500`).bind(start,end).all<Row>();let synchronized=0;for(const row of rows.results??[]){const p=ACTIVITY_REMINDER_KINDS.map(()=>"?").join(",");const exists=await env.DB.prepare(`SELECT id FROM notifications WHERE entity='tasks' AND entity_id=? AND kind IN (${p}) AND dismissed_at IS NULL LIMIT 1`).bind(row.id,...ACTIVITY_REMINDER_KINDS).first();if(exists)continue;await syncEventReminders({taskId:row.id,title:row.title,dueDate:row.due_date,dueTime:row.due_time,taskTypeId:row.task_type_id,itemKind:row.item_kind??"task",startsAt:row.starts_at,endsAt:row.ends_at});synchronized++;}return{scanned:rows.results?.length??0,synchronized};}
